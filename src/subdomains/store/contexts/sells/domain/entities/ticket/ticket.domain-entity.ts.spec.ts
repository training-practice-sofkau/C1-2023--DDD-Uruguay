import { TicketDomainEntityTs } from './ticket.domain-entity.ts';

describe('TicketDomainEntityTs', () => {
  it('should be defined', () => {
    expect(new TicketDomainEntityTs()).toBeDefined();
  });
});

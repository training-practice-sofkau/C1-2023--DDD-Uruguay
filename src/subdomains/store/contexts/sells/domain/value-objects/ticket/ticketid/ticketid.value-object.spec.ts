import { TicketidValueObject } from './ticketid.value-object';

describe('TicketidValueObject', () => {
  it('should be defined', () => {
    expect(new TicketidValueObject()).toBeDefined();
  });
});

import { QuantityValueObject } from './quantity.value-object';

describe('QuantityValueObject', () => {
  it('should be defined', () => {
    expect(new QuantityValueObject()).toBeDefined();
  });
});

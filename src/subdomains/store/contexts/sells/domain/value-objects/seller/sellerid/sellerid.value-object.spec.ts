import { SelleridValueObject } from './sellerid.value-object';

describe('SelleridValueObject', () => {
  it('should be defined', () => {
    expect(new SelleridValueObject()).toBeDefined();
  });
});

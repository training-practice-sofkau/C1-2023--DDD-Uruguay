import { ExpirationDateValueObject } from './expiration-date.value-object';

describe('ExpirationDateValueObject', () => {
  it('should be defined', () => {
    expect(new ExpirationDateValueObject()).toBeDefined();
  });
});

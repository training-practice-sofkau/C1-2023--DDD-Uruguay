import { ProductidValueObject } from './productid.value-object';

describe('ProductidValueObject', () => {
  it('should be defined', () => {
    expect(new ProductidValueObject()).toBeDefined();
  });
});

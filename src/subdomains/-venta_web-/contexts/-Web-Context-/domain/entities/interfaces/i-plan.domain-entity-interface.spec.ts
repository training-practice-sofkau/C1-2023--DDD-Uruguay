import { IPlanDomainEntityInterface } from './i-plan.domain-entity-interface';

describe('IPlanDomainEntityInterface', () => {
  it('should be defined', () => {
    expect(new IPlanDomainEntityInterface()).toBeDefined();
  });
});

import { MembershipDomainEntity } from './membership.domain-entity';

describe('MembershipDomainEntity', () => {
  it('should be defined', () => {
    expect(new MembershipDomainEntity()).toBeDefined();
  });
});

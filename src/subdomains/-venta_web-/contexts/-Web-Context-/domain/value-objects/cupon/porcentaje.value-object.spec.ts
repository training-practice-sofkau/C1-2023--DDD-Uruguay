import { PorcentajeValueObject } from './porcentaje.value-object';

describe('PorcentajeValueObject', () => {
  it('should be defined', () => {
    expect(new PorcentajeValueObject()).toBeDefined();
  });
});

import { UpdateClientPhoneUseCase } from './update-client-phone.use-case';

describe('UpdateClientPhoneUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateClientPhoneUseCase()).toBeDefined();
  });
});

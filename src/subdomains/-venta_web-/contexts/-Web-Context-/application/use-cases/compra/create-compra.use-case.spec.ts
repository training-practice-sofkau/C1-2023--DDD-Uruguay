import { CreateCompraUseCase } from './create-compra.use-case';

describe('CreateCompraUseCase', () => {
  it('should be defined', () => {
    expect(new CreateCompraUseCase()).toBeDefined();
  });
});

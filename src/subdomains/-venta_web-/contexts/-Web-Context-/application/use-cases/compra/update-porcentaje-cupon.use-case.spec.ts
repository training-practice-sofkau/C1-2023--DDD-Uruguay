import { UpdatePorcentajeCuponUseCase } from './update-porcentaje-cupon.use-case';

describe('UpdatePorcentajeCuponUseCase', () => {
  it('should be defined', () => {
    expect(new UpdatePorcentajeCuponUseCase()).toBeDefined();
  });
});

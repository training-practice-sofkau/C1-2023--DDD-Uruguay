import { UpdateNombrePlanUseCase } from './update-nombre-plan.use-case';

describe('UpdateNombrePlanUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateNombrePlanUseCase()).toBeDefined();
  });
});

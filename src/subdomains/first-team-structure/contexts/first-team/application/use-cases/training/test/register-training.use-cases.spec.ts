import { RegisterTrainingUseCases } from '../register-training.use-cases';

describe('RegisterTrainingUseCases', () => {
  it('should be defined', () => {
    expect(new RegisterTrainingUseCases()).toBeDefined();
  });
});

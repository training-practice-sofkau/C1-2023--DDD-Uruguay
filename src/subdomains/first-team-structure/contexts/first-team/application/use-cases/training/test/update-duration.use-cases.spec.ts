import { UpdateDurationUseCases } from './update-duration.use-cases';

describe('UpdateDurationUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateDurationUseCases()).toBeDefined();
  });
});

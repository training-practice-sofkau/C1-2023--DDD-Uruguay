import { UpdateWorkoutGoalUseCases } from './update-workout-goal.use-cases';

describe('UpdateWorkoutGoalUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateWorkoutGoalUseCases()).toBeDefined();
  });
});

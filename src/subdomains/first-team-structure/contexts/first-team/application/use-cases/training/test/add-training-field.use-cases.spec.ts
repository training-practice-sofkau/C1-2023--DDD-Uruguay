import { AddTrainingFieldUseCases } from './add-training-field.use-cases';

describe('AddTrainingFieldUseCases', () => {
  it('should be defined', () => {
    expect(new AddTrainingFieldUseCases()).toBeDefined();
  });
});

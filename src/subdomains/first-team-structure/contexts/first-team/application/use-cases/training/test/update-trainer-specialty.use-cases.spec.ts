import { UpdateTrainerSpecialtyUseCases } from './update-trainer-specialty.use-cases';

describe('UpdateTrainerSpecialtyUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateTrainerSpecialtyUseCases()).toBeDefined();
  });
});

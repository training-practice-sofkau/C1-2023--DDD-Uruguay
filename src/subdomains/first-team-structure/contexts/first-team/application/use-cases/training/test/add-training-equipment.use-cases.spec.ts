import { AddTrainingEquipmentUseCases } from './add-training-equipment.use-cases';

describe('AddTrainingEquipmentUseCases', () => {
  it('should be defined', () => {
    expect(new AddTrainingEquipmentUseCases()).toBeDefined();
  });
});

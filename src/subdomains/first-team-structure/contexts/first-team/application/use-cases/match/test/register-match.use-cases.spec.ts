import { RegisterMatchUseCases } from './register-match.use-cases';

describe('RegisterMatchUseCases', () => {
  it('should be defined', () => {
    expect(new RegisterMatchUseCases()).toBeDefined();
  });
});

import { UpdateStadiumCapacityUseCases } from './update-stadium-capacity.use-cases';

describe('UpdateStadiumCapacityUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateStadiumCapacityUseCases()).toBeDefined();
  });
});

import { UpdateRivalTownUseCases } from './update-rival-town.use-cases';

describe('UpdateRivalTownUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateRivalTownUseCases()).toBeDefined();
  });
});

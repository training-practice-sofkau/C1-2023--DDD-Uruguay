import { UpdatePlayerPositionUseCases } from './update-player-position.use-cases';

describe('UpdatePlayerPositionUseCases', () => {
  it('should be defined', () => {
    expect(new UpdatePlayerPositionUseCases()).toBeDefined();
  });
});

import { UpdatePlayerWageUseCases } from './update-player-wage.use-cases';

describe('UpdatePlayerWageUseCases', () => {
  it('should be defined', () => {
    expect(new UpdatePlayerWageUseCases()).toBeDefined();
  });
});

import { RegisterTeamUseCases } from './register-team.use-cases';

describe('RegisterTeamUseCases', () => {
  it('should be defined', () => {
    expect(new RegisterTeamUseCases()).toBeDefined();
  });
});

import { UpdateCoachWageUseCases } from './update-coach-wage.use-cases';

describe('UpdateCoachWageUseCases', () => {
  it('should be defined', () => {
    expect(new UpdateCoachWageUseCases()).toBeDefined();
  });
});

import { CapacityValueObject } from './capacity.value-object';

describe('CapacityValueObject', () => {
  it('should be defined', () => {
    expect(new CapacityValueObject()).toBeDefined();
  });
});

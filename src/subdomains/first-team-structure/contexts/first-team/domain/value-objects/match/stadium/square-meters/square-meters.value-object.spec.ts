import { SquareMetersValueObject } from './square-meters.value-object';

describe('SquareMetersValueObject', () => {
  it('should be defined', () => {
    expect(new SquareMetersValueObject()).toBeDefined();
  });
});

import { TrainingFieldIdValueObject } from './training-field-id.value-object';

describe('TrainingFieldIdValueObject', () => {
  it('should be defined', () => {
    expect(new TrainingFieldIdValueObject()).toBeDefined();
  });
});

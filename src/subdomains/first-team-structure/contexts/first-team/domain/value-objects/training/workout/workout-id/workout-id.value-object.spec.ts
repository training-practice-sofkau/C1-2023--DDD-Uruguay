import { WorkoutIdValueObject } from './workout-id.value-object';

describe('WorkoutIdValueObject', () => {
  it('should be defined', () => {
    expect(new WorkoutIdValueObject()).toBeDefined();
  });
});

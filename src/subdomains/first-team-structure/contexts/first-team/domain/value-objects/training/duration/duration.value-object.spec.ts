import { DurationValueObject } from './duration.value-object';

describe('DurationValueObject', () => {
  it('should be defined', () => {
    expect(new DurationValueObject()).toBeDefined();
  });
});

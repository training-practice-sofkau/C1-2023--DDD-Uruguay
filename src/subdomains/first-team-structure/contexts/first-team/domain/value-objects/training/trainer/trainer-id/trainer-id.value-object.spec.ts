import { TrainerIdValueObject } from './trainer-id.value-object';

describe('TrainerIdValueObject', () => {
  it('should be defined', () => {
    expect(new TrainerIdValueObject()).toBeDefined();
  });
});

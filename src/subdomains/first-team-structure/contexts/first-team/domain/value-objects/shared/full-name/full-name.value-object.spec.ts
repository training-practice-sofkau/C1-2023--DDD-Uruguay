import { FullNameValueObject } from './full-name.value-object';

describe('FullNameValueObject', () => {
  it('should be defined', () => {
    expect(new FullNameValueObject()).toBeDefined();
  });
});

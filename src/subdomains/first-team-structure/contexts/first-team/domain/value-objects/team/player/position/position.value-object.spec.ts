import { PositionValueObject } from './position.value-object';

describe('PositionValueObject', () => {
  it('should be defined', () => {
    expect(new PositionValueObject()).toBeDefined();
  });
});

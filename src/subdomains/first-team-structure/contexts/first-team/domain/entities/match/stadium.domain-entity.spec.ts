import { StadiumDomainEntity } from './stadium.domain-entity';

describe('StadiumDomainEntity', () => {
  it('should be defined', () => {
    expect(new StadiumDomainEntity()).toBeDefined();
  });
});

import { TrainingEquipmentDomainEntity } from './training-equipment.domain-entity';

describe('TrainingEquipmentDomainEntity', () => {
  it('should be defined', () => {
    expect(new TrainingEquipmentDomainEntity()).toBeDefined();
  });
});

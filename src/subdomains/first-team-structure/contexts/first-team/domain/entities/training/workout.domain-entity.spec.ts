import { WorkoutDomainEntity } from './workout.domain-entity';

describe('WorkoutDomainEntity', () => {
  it('should be defined', () => {
    expect(new WorkoutDomainEntity()).toBeDefined();
  });
});

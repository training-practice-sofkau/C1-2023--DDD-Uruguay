import { TrainingDomainEntity } from './training.domain-entity';

describe('TrainingDomainEntity', () => {
  it('should be defined', () => {
    expect(new TrainingDomainEntity()).toBeDefined();
  });
});

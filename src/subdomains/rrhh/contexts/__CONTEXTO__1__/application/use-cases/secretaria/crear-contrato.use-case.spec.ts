import { CrearContratoUseCase } from './crear-contrato.use-case';

describe('CrearContratoUseCase', () => {
  it('should be defined', () => {
    expect(new CrearContratoUseCase()).toBeDefined();
  });
});

import { RemoverEmpleadoUseCase } from './remover-empleado.use-case';

describe('RemoverEmpleadoUseCase', () => {
  it('should be defined', () => {
    expect(new RemoverEmpleadoUseCase()).toBeDefined();
  });
});

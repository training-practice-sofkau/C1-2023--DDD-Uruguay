import { ModificarStateCommands } from './modificar-state.commands';

describe('ModificarStateCommands', () => {
  it('should be defined', () => {
    expect(new ModificarStateCommands()).toBeDefined();
  });
});

import { ModificarTramiteCommands } from './modificar-tramite.commands';

describe('ModificarTramiteCommands', () => {
  it('should be defined', () => {
    expect(new ModificarTramiteCommands()).toBeDefined();
  });
});

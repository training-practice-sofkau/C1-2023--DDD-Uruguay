import { CrearTramiteCommands } from './crear-tramite.commands';

describe('CrearTramiteCommands', () => {
  it('should be defined', () => {
    expect(new CrearTramiteCommands()).toBeDefined();
  });
});

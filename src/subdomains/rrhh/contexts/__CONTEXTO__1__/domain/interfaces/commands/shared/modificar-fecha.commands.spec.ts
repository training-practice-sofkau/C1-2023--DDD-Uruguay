import { ModificarFechaCommands } from './modificar-fecha.commands';

describe('ModificarFechaCommands', () => {
  it('should be defined', () => {
    expect(new ModificarFechaCommands()).toBeDefined();
  });
});

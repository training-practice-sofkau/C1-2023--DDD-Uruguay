import { CrearEmpleadoCommands } from './crear-empleado.commands';

describe('CrearEmpleadoCommands', () => {
  it('should be defined', () => {
    expect(new CrearEmpleadoCommands()).toBeDefined();
  });
});

import { ModificarSalarioCommands } from './modificar-salario.commands';

describe('ModificarSalarioCommands', () => {
  it('should be defined', () => {
    expect(new ModificarSalarioCommands()).toBeDefined();
  });
});

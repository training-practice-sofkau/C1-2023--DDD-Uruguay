import { BuscarEmpleadoCommands } from './buscar-empleado.commands';

describe('BuscarEmpleadoCommands', () => {
  it('should be defined', () => {
    expect(new BuscarEmpleadoCommands()).toBeDefined();
  });
});

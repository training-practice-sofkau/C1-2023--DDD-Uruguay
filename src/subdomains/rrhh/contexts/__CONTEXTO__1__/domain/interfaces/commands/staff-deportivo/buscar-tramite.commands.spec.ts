import { BuscarTramiteCommands } from './buscar-tramite.commands';

describe('BuscarTramiteCommands', () => {
  it('should be defined', () => {
    expect(new BuscarTramiteCommands()).toBeDefined();
  });
});

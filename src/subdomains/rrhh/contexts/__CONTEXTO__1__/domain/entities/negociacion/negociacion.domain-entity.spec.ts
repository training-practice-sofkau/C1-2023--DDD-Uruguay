import { NegociacionDomainEntity } from './negociacion.domain-entity';

describe('NegociacionDomainEntity', () => {
  it('should be defined', () => {
    expect(new NegociacionDomainEntity()).toBeDefined();
  });
});

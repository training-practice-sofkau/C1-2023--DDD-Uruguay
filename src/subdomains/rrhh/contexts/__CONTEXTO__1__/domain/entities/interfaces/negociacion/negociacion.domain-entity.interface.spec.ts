import { NegociacionDomainEntityInterface } from './negociacion.domain-entity.interface';

describe('NegociacionDomainEntityInterface', () => {
  it('should be defined', () => {
    expect(new NegociacionDomainEntityInterface()).toBeDefined();
  });
});

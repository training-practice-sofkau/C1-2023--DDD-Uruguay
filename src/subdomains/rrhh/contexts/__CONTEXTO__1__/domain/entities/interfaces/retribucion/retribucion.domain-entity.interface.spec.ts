import { RetribucionDomainEntityInterface } from './retribucion.domain-entity.interface';

describe('RetribucionDomainEntityInterface', () => {
  it('should be defined', () => {
    expect(new RetribucionDomainEntityInterface()).toBeDefined();
  });
});

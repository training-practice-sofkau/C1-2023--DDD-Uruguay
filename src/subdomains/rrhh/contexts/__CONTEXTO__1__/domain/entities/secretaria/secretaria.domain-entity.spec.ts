import { SecretariaDomainEntity } from './secretaria.domain-entity';

describe('SecretariaDomainEntity', () => {
  it('should be defined', () => {
    expect(new SecretariaDomainEntity()).toBeDefined();
  });
});

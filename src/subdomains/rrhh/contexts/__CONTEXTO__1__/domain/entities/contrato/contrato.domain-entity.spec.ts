import { ContratoDomainEntity } from './contrato.domain-entity';

describe('ContratoDomainEntity', () => {
  it('should be defined', () => {
    expect(new ContratoDomainEntity()).toBeDefined();
  });
});

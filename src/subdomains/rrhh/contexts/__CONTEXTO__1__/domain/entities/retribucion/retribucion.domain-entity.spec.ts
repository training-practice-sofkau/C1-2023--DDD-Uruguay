import { RetribucionDomainEntity } from './retribucion.domain-entity';

describe('RetribucionDomainEntity', () => {
  it('should be defined', () => {
    expect(new RetribucionDomainEntity()).toBeDefined();
  });
});

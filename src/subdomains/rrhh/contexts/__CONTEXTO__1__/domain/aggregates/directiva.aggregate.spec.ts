import { DirectivaAggregate } from './directiva.aggregate/directiva.aggregate';

describe('DirectivaAggregate', () => {
  it('should be defined', () => {
    expect(new DirectivaAggregate()).toBeDefined();
  });
});

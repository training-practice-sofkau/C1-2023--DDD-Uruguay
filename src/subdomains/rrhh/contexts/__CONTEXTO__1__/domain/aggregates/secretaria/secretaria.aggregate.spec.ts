import { SecretariaAggregate } from './secretaria.aggregate';

describe('SecretariaAggregate', () => {
  it('should be defined', () => {
    expect(new SecretariaAggregate()).toBeDefined();
  });
});

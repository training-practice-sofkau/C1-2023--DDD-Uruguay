import { DocumentoModificadoEventPublisher } from './documento-modificado.event-publisher';

describe('DocumentoModificadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new DocumentoModificadoEventPublisher()).toBeDefined();
  });
});

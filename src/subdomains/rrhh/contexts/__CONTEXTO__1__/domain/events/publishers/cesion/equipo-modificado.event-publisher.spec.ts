import { EquipoModificadoEventPublisher } from './equipo-modificado.event-publisher';

describe('EquipoModificadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new EquipoModificadoEventPublisher()).toBeDefined();
  });
});

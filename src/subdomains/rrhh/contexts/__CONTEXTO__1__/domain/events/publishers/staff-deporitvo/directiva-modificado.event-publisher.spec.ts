import { DirectivaModificadoEventPublisher } from './directiva-modificado.event-publisher';

describe('DirectivaModificadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new DirectivaModificadoEventPublisher()).toBeDefined();
  });
});

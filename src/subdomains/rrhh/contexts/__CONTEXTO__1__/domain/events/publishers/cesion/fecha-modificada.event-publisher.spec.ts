import { FechaModificadaEventPublisher } from './fecha-modificada.event-publisher';

describe('FechaModificadaEventPublisher', () => {
  it('should be defined', () => {
    expect(new FechaModificadaEventPublisher()).toBeDefined();
  });
});

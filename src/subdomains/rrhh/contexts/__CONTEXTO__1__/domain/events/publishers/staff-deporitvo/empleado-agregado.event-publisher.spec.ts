import { EmpleadoAgregadoEventPublisher } from './empleado-agregado.event-publisher';

describe('EmpleadoAgregadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new EmpleadoAgregadoEventPublisher()).toBeDefined();
  });
});

import { StateModificadoEventPublisher } from './state-modificado.event-publisher';

describe('StateModificadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new StateModificadoEventPublisher()).toBeDefined();
  });
});

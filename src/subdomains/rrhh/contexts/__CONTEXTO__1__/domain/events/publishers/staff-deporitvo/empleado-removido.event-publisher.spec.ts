import { EmpleadoRemovidoEventPublisher } from './empleado-removido.event-publisher';

describe('EmpleadoRemovidoEventPublisher', () => {
  it('should be defined', () => {
    expect(new EmpleadoRemovidoEventPublisher()).toBeDefined();
  });
});

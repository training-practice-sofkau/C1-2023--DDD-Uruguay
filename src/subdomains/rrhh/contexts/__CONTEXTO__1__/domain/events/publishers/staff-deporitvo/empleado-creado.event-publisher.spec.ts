import { EmpleadoCreadoEventPublisher } from './empleado-creado.event-publisher';

describe('EmpleadoCreadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new EmpleadoCreadoEventPublisher()).toBeDefined();
  });
});

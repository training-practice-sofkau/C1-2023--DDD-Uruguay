import { TramiteAgregadoEventPublisher } from './tramite-agregado.event-publisher';

describe('TramiteAgregadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new TramiteAgregadoEventPublisher()).toBeDefined();
  });
});

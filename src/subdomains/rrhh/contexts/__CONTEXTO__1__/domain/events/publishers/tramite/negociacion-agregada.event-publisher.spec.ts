import { NegociacionAgregadaEventPublisher } from './negociacion-agregada.event-publisher';

describe('NegociacionAgregadaEventPublisher', () => {
  it('should be defined', () => {
    expect(new NegociacionAgregadaEventPublisher()).toBeDefined();
  });
});

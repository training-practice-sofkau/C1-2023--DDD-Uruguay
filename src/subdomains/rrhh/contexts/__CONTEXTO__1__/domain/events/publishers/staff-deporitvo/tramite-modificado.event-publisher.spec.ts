import { TramiteModificadoEventPublisher } from './tramite-modificado.event-publisher';

describe('TramiteModificadoEventPublisher', () => {
  it('should be defined', () => {
    expect(new TramiteModificadoEventPublisher()).toBeDefined();
  });
});

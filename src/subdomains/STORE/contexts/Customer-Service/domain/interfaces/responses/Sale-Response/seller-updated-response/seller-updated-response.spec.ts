import { SellerUpdatedResponse } from './seller-updated-response';

describe('SellerUpdatedResponse', () => {
  it('should be defined', () => {
    expect(new SellerUpdatedResponse()).toBeDefined();
  });
});

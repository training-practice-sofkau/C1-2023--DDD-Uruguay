import { BillUpdatedResponse } from './bill-updated-response';

describe('BillUpdatedResponse', () => {
  it('should be defined', () => {
    expect(new BillUpdatedResponse()).toBeDefined();
  });
});

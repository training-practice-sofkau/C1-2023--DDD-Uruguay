import { TotalUpdatedResponse } from './total--updated-response';

describe('TotalUpdatedResponse', () => {
  it('should be defined', () => {
    expect(new TotalUpdatedResponse()).toBeDefined();
  });
});

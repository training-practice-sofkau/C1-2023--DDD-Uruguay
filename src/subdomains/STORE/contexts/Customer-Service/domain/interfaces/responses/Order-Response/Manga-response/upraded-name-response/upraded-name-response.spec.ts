import { UpradedNameResponse } from './upraded-name-response';

describe('UpradedNameResponse', () => {
  it('should be defined', () => {
    expect(new UpradedNameResponse()).toBeDefined();
  });
});

import { UpradedPriceResponse } from './upraded-price-response';

describe('UpradedPriceResponse', () => {
  it('should be defined', () => {
    expect(new UpradedPriceResponse()).toBeDefined();
  });
});

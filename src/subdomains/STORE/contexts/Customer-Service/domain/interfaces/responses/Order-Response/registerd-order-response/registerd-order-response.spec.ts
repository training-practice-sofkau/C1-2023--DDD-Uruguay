import { RegisterdOrderResponse } from './registerd-order-response';

describe('RegisterdOrderResponse', () => {
  it('should be defined', () => {
    expect(new RegisterdOrderResponse()).toBeDefined();
  });
});

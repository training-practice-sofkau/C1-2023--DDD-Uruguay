import { AddedSallerResponse } from './added-saller-response';

describe('AddedSallerResponse', () => {
  it('should be defined', () => {
    expect(new AddedSallerResponse()).toBeDefined();
  });
});

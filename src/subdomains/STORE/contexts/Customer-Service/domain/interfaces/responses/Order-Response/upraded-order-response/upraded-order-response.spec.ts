import { UpradedOrderResponse } from './upraded-order-response';

describe('UpradedOrderResponse', () => {
  it('should be defined', () => {
    expect(new UpradedOrderResponse()).toBeDefined();
  });
});

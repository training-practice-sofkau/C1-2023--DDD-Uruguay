import { UpdatePaymentMethod } from './update-payment-method';

describe('UpdatePaymentMethod', () => {
  it('should be defined', () => {
    expect(new UpdatePaymentMethod()).toBeDefined();
  });
});

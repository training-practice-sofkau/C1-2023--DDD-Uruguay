import { UpdateNameSallerUseCase } from './update-name-saller-use-case';

describe('UpdateNameSallerUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateNameSallerUseCase()).toBeDefined();
  });
});

import { RegisterSaleUseCase } from './register-sale-use-case';

describe('RegisterSaleUseCase', () => {
  it('should be defined', () => {
    expect(new RegisterSaleUseCase()).toBeDefined();
  });
});

import { DeviceTypeValueObject } from './device-type.value-object';

describe('DeviceTypeValueObject', () => {
  it('should be defined', () => {
    expect(new DeviceTypeValueObject()).toBeDefined();
  });
});

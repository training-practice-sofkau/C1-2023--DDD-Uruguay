import { ChangeCustomerFullnameEventPublisher } from './change-customer-fullname.event-publisher';

describe('ChangeCustomerFullnameEventPublisher', () => {
  it('should be defined', () => {
    expect(new ChangeCustomerFullnameEventPublisher()).toBeDefined();
  });
});

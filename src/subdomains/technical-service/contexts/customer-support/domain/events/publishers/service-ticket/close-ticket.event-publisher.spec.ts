import { CloseTicketEventPublisher } from './close-ticket.event-publisher';

describe('CloseTicketEventPublisher', () => {
  it('should be defined', () => {
    expect(new CloseTicketEventPublisher()).toBeDefined();
  });
});

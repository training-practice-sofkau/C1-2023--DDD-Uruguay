import { CancelWarrantyEventPublisher } from './cancel-warranty.event-publisher';

describe('CancelWarrantyEventPublisher', () => {
  it('should be defined', () => {
    expect(new CancelWarrantyEventPublisher()).toBeDefined();
  });
});

import { ChangeDeviceInformationEventPublisher } from './change-device-information.event-publisher';

describe('ChangeDeviceInformationEventPublisher', () => {
  it('should be defined', () => {
    expect(new ChangeDeviceInformationEventPublisher()).toBeDefined();
  });
});

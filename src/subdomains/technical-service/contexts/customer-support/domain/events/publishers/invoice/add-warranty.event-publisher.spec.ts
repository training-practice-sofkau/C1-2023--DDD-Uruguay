import { AddWarrantyEventPublisher } from './add-warranty.event-publisher';

describe('AddWarrantyEventPublisher', () => {
  it('should be defined', () => {
    expect(new AddWarrantyEventPublisher()).toBeDefined();
  });
});

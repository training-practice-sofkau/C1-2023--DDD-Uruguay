import { ChangeEmployeeFullnameEventPublisher } from './change-employee-fullname.event-publisher';

describe('ChangeEmployeeFullnameEventPublisher', () => {
  it('should be defined', () => {
    expect(new ChangeEmployeeFullnameEventPublisher()).toBeDefined();
  });
});

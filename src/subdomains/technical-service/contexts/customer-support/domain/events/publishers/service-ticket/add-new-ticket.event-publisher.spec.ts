import { AddNewTicketEventPublisher } from './add-new-ticket.event-publisher';

describe('AddNewTicketEventPublisher', () => {
  it('should be defined', () => {
    expect(new AddNewTicketEventPublisher()).toBeDefined();
  });
});

import { ChangeCustomerPhoneEventPublisher } from './change-customer-phone.event-publisher';

describe('ChangeCustomerPhoneEventPublisher', () => {
  it('should be defined', () => {
    expect(new ChangeCustomerPhoneEventPublisher()).toBeDefined();
  });
});

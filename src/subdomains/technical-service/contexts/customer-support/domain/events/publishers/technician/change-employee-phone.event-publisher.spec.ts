import { ChangeEmployeePhoneEventPublisher } from './change-employee-phone.event-publisher';

describe('ChangeEmployeePhoneEventPublisher', () => {
  it('should be defined', () => {
    expect(new ChangeEmployeePhoneEventPublisher()).toBeDefined();
  });
});

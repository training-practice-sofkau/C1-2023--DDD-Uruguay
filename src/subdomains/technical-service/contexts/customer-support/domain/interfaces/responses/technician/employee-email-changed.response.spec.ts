import { EmployeeEmailChangedResponse } from './employee-email-changed.response';

describe('EmployeeEmailChangedResponse', () => {
  it('should be defined', () => {
    expect(new EmployeeEmailChangedResponse()).toBeDefined();
  });
});

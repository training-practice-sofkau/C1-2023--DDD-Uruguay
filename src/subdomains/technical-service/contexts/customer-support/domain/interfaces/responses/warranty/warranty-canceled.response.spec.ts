import { WarrantyCanceledResponse } from './warranty-canceled.response';

describe('WarrantyCanceledResponse', () => {
  it('should be defined', () => {
    expect(new WarrantyCanceledResponse()).toBeDefined();
  });
});

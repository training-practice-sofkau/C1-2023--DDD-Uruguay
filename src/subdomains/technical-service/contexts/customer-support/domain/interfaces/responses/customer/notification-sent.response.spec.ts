import { NotificationSentResponse } from './notification-sent.response';

describe('NotificationSentResponse', () => {
  it('should be defined', () => {
    expect(new NotificationSentResponse()).toBeDefined();
  });
});

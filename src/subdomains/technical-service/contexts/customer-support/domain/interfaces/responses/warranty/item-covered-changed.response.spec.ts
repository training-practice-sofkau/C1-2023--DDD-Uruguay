import { ItemCoveredChangedResponse } from './item-covered-changed.response';

describe('ItemCoveredChangedResponse', () => {
  it('should be defined', () => {
    expect(new ItemCoveredChangedResponse()).toBeDefined();
  });
});

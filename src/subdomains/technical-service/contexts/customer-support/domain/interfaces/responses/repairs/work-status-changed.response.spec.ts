import { WorkStatusChangedResponse } from './work-status-changed.response';

describe('WorkStatusChangedResponse', () => {
  it('should be defined', () => {
    expect(new WorkStatusChangedResponse()).toBeDefined();
  });
});

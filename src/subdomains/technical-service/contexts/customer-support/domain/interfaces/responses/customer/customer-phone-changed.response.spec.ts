import { CustomerPhoneChangedResponse } from './customer-phone-changed.response';

describe('CustomerPhoneChangedResponse', () => {
  it('should be defined', () => {
    expect(new CustomerPhoneChangedResponse()).toBeDefined();
  });
});

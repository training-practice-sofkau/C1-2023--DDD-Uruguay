import { ServiceChargeCalculatedResponse } from './service-charge-calculated.response';

describe('ServiceChargeCalculatedResponse', () => {
  it('should be defined', () => {
    expect(new ServiceChargeCalculatedResponse()).toBeDefined();
  });
});

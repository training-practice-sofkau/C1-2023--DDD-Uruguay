import { EmployeeStatusChangedResponse } from './employee-status-changed.response';

describe('EmployeeStatusChangedResponse', () => {
  it('should be defined', () => {
    expect(new EmployeeStatusChangedResponse()).toBeDefined();
  });
});

import { NewTicketOpenedResponse } from './new-ticket-opened.response';

describe('NewTicketOpenedResponse', () => {
  it('should be defined', () => {
    expect(new NewTicketOpenedResponse()).toBeDefined();
  });
});

import { EmployeePhoneChangedResponse } from './employee-phone-changed.response';

describe('EmployeePhoneChangedResponse', () => {
  it('should be defined', () => {
    expect(new EmployeePhoneChangedResponse()).toBeDefined();
  });
});

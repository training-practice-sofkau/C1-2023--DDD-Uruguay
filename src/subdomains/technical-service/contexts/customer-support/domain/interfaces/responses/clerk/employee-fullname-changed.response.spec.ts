import { EmployeeFullnameChangedResponse } from './employee-fullname-changed.response';

describe('EmployeeFullnameChangedResponse', () => {
  it('should be defined', () => {
    expect(new EmployeeFullnameChangedResponse()).toBeDefined();
  });
});

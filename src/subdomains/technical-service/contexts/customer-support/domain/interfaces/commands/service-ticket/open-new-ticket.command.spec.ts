import { OpenNewTicketCommand } from './open-new-ticket.command';

describe('OpenNewTicketCommand', () => {
  it('should be defined', () => {
    expect(new OpenNewTicketCommand()).toBeDefined();
  });
});

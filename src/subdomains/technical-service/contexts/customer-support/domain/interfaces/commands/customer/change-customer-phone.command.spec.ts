import { ChangeCustomerPhoneCommand } from './change-customer-phone.command';

describe('ChangeCustomerPhoneCommand', () => {
  it('should be defined', () => {
    expect(new ChangeCustomerPhoneCommand()).toBeDefined();
  });
});

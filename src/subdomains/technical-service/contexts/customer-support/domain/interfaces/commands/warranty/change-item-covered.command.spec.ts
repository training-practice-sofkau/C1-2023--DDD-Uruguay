import { ChangeItemCoveredCommand } from './change-item-covered.command';

describe('ChangeItemCoveredCommand', () => {
  it('should be defined', () => {
    expect(new ChangeItemCoveredCommand()).toBeDefined();
  });
});

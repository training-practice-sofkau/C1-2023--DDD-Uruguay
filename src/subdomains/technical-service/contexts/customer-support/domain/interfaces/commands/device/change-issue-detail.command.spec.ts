import { ChangeIssueDetailCommand } from './change-issue-detail.command';

describe('ChangeIssueDetailCommand', () => {
  it('should be defined', () => {
    expect(new ChangeIssueDetailCommand()).toBeDefined();
  });
});

import { CancelWarrantyCommand } from './cancel-warranty.command';

describe('CancelWarrantyCommand', () => {
  it('should be defined', () => {
    expect(new CancelWarrantyCommand()).toBeDefined();
  });
});

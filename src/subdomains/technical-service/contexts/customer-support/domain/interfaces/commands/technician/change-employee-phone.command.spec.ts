import { ChangeEmployeePhoneCommand } from './change-employee-phone.command';

describe('ChangeEmployeePhoneCommand', () => {
  it('should be defined', () => {
    expect(new ChangeEmployeePhoneCommand()).toBeDefined();
  });
});

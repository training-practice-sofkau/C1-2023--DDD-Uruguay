import { ChangeEmployeeFullnameCommand } from './change-employee-fullname.command';

describe('ChangeEmployeeFullnameCommand', () => {
  it('should be defined', () => {
    expect(new ChangeEmployeeFullnameCommand()).toBeDefined();
  });
});

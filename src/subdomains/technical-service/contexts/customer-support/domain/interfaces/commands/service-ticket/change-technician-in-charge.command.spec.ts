import { ChangeTechnicianInChargeCommand } from './change-technician-in-charge.command';

describe('ChangeTechnicianInChargeCommand', () => {
  it('should be defined', () => {
    expect(new ChangeTechnicianInChargeCommand()).toBeDefined();
  });
});

import { ChangeWorkStatusCommand } from './change-work-status.command';

describe('ChangeWorkStatusCommand', () => {
  it('should be defined', () => {
    expect(new ChangeWorkStatusCommand()).toBeDefined();
  });
});

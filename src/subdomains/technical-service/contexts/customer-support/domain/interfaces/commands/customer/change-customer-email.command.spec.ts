import { ChangeCustomerEmailCommand } from './change-customer-email.command';

describe('ChangeCustomerEmailCommand', () => {
  it('should be defined', () => {
    expect(new ChangeCustomerEmailCommand()).toBeDefined();
  });
});

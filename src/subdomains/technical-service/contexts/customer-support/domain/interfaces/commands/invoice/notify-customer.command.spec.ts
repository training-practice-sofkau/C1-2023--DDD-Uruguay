import { NotifyCustomerCommand } from './notify-customer.command';

describe('NotifyCustomerCommand', () => {
  it('should be defined', () => {
    expect(new NotifyCustomerCommand()).toBeDefined();
  });
});

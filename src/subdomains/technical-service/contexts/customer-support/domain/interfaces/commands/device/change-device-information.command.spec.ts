import { ChangeDeviceInformationCommand } from './change-device-information.command';

describe('ChangeDeviceInformationCommand', () => {
  it('should be defined', () => {
    expect(new ChangeDeviceInformationCommand()).toBeDefined();
  });
});

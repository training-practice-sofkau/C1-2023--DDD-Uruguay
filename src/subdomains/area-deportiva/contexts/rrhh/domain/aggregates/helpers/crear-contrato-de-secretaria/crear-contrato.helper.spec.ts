import { CrearContratoHelper } from './crear-contrato.helper';

describe('CrearContratoHelper', () => {
  it('should be defined', () => {
    expect(new CrearContratoHelper()).toBeDefined();
  });
});

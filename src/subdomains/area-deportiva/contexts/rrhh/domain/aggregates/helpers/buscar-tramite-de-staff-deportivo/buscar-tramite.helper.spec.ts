import { BuscarTramiteHelper } from './buscar-tramite.helper';

describe('BuscarTramiteHelper', () => {
  it('should be defined', () => {
    expect(new BuscarTramiteHelper()).toBeDefined();
  });
});

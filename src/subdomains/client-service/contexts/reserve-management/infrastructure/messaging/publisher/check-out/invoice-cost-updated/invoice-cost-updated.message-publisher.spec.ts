import { InvoiceCostUpdatedMessagePublisher } from './invoice-cost-updated.message-publisher';

describe('InvoiceCostUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new InvoiceCostUpdatedMessagePublisher()).toBeDefined();
  });
});

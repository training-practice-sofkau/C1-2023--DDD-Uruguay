import { RoomKeyAddedMessagePublisher } from './room-key-added.message-publisher';

describe('RoomKeyAddedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new RoomKeyAddedMessagePublisher()).toBeDefined();
  });
});

import { CostUpdatedMessagePublisher } from './cost-updated.message-publisher';

describe('CostUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new CostUpdatedMessagePublisher()).toBeDefined();
  });
});

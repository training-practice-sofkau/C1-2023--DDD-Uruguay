import { InvoiceAddedMessagePublisher } from './invoice-added.message-publisher';

describe('InvoiceAddedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new InvoiceAddedMessagePublisher()).toBeDefined();
  });
});

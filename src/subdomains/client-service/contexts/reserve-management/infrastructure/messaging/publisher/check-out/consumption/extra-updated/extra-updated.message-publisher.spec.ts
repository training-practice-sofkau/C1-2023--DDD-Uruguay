import { ExtraUpdatedMessagePublisher } from './extra-updated.message-publisher';

describe('ExtraUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new ExtraUpdatedMessagePublisher()).toBeDefined();
  });
});

import { EmailUpdatedMessagePublisher } from './email-updated.message-publisher';

describe('EmailUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new EmailUpdatedMessagePublisher()).toBeDefined();
  });
});

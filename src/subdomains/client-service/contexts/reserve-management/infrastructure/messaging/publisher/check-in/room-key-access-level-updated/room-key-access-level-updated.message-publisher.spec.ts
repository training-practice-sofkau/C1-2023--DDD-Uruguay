import { RoomKeyAccessLevelUpdatedMessagePublisher } from './room-key-access-level-updated.message-publisher';

describe('RoomKeyAccessLevelUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new RoomKeyAccessLevelUpdatedMessagePublisher()).toBeDefined();
  });
});

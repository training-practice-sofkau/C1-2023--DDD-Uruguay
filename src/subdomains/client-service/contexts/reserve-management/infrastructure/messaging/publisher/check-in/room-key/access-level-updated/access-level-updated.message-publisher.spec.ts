import { AccessLevelUpdatedMessagePublisher } from './access-level-updated.message-publisher';

describe('AccessLevelUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new AccessLevelUpdatedMessagePublisher()).toBeDefined();
  });
});

import { EndDateUpdatedMessagePublisher } from './end-date-updated.message-publisher';

describe('EndDateUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new EndDateUpdatedMessagePublisher()).toBeDefined();
  });
});

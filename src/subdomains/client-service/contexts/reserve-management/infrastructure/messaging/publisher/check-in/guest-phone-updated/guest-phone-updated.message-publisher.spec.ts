import { GuestPhoneUpdatedMessagePublisher } from './guest-phone-updated.message-publisher';

describe('GuestPhoneUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new GuestPhoneUpdatedMessagePublisher()).toBeDefined();
  });
});

import { StartDateUpdatedMessagePublisher } from './start-date-updated.message-publisher';

describe('StartDateUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new StartDateUpdatedMessagePublisher()).toBeDefined();
  });
});

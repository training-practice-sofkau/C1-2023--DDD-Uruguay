import { NumberOfGuestUpdatedMessagePublisher } from './number-of-guest-updated.message-publisher';

describe('NumberOfGuestUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new NumberOfGuestUpdatedMessagePublisher()).toBeDefined();
  });
});

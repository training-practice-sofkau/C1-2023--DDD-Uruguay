import { ConsumptionAddedMessagePublisher } from './consumption-added.message-publisher';

describe('ConsumptionAddedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new ConsumptionAddedMessagePublisher()).toBeDefined();
  });
});

import { CustomerAddedMessagePublisher } from './customer-added.message-publisher';

describe('CustomerAddedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new CustomerAddedMessagePublisher()).toBeDefined();
  });
});

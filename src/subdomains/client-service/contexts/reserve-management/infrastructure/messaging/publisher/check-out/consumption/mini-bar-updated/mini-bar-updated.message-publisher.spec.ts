import { MiniBarUpdatedMessagePublisher } from './mini-bar-updated.message-publisher';

describe('MiniBarUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new MiniBarUpdatedMessagePublisher()).toBeDefined();
  });
});

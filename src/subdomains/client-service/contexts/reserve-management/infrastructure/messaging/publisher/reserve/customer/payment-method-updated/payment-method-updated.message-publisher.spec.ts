import { PaymentMethodUpdatedMessagePublisher } from './payment-method-updated.message-publisher';

describe('PaymentMethodUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new PaymentMethodUpdatedMessagePublisher()).toBeDefined();
  });
});

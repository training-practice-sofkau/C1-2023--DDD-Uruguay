import { PhoneUpdatedMessagePublisher } from './phone-updated.message-publisher';

describe('PhoneUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new PhoneUpdatedMessagePublisher()).toBeDefined();
  });
});

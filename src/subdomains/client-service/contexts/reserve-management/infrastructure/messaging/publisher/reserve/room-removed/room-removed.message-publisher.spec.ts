import { RoomRemovedMessagePublisher } from './room-removed.message-publisher';

describe('RoomRemovedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new RoomRemovedMessagePublisher()).toBeDefined();
  });
});

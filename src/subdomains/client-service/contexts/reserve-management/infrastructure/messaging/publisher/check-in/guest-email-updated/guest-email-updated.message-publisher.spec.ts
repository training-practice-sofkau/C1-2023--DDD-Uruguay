import { GuestEmailUpdatedMessagePublisher } from './guest-email-updated.message-publisher';

describe('GuestEmailUpdatedMessagePublisher', () => {
  it('should be defined', () => {
    expect(new GuestEmailUpdatedMessagePublisher()).toBeDefined();
  });
});

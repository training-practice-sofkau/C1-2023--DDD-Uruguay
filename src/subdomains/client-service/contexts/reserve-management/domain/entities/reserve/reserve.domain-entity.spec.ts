import { ReserveDomainEntity } from './reserve.domain-entity';

describe('ReserveDomainEntity', () => {
  it('should be defined', () => {
    expect(new ReserveDomainEntity()).toBeDefined();
  });
});

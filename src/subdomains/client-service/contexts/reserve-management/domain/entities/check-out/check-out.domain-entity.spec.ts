import { CheckOutDomainEntity } from './check-out.domain-entity';

describe('CheckOutDomainEntity', () => {
  it('should be defined', () => {
    expect(new CheckOutDomainEntity()).toBeDefined();
  });
});

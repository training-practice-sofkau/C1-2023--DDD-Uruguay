import { InvoiceDomainEntity } from './invoice.domain-entity';

describe('InvoiceDomainEntity', () => {
  it('should be defined', () => {
    expect(new InvoiceDomainEntity()).toBeDefined();
  });
});

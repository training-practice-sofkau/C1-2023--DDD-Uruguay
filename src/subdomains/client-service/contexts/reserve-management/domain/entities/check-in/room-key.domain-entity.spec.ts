import { RoomKeyDomainEntity } from './room-key.domain-entity';

describe('RoomKeyDomainEntity', () => {
  it('should be defined', () => {
    expect(new RoomKeyDomainEntity()).toBeDefined();
  });
});

import { CheckInDomainEntity } from './check-in.domain-entity';

describe('CheckInDomainEntity', () => {
  it('should be defined', () => {
    expect(new CheckInDomainEntity()).toBeDefined();
  });
});

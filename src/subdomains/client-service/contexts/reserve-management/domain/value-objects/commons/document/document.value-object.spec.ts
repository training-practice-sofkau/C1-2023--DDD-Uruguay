import { DocumentValueObject } from './document.value-object';

describe('DocumentValueObject', () => {
  it('should be defined', () => {
    expect(new DocumentValueObject()).toBeDefined();
  });
});

import { NumberOfGuestsValueObject } from './number-of-guests.value-object';

describe('NumberOfGuestsValueObject', () => {
  it('should be defined', () => {
    expect(new NumberOfGuestsValueObject()).toBeDefined();
  });
});

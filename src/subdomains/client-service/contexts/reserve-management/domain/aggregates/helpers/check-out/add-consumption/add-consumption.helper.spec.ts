import { AddConsumptionHelper } from './add-consumption.helper';

describe('AddConsumptionHelper', () => {
  it('should be defined', () => {
    expect(new AddConsumptionHelper()).toBeDefined();
  });
});

import { UpdateEndDateHelper } from './update-end-date.helper';

describe('UpdateEndDateHelper', () => {
  it('should be defined', () => {
    expect(new UpdateEndDateHelper()).toBeDefined();
  });
});

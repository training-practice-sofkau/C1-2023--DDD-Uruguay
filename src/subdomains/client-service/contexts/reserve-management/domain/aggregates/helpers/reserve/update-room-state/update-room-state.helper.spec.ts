import { UpdateRoomStateHelper } from './update-room-state.helper';

describe('UpdateRoomStateHelper', () => {
  it('should be defined', () => {
    expect(new UpdateRoomStateHelper()).toBeDefined();
  });
});

import { UpdateGuestPhoneHelper } from './update-guest-phone.helper';

describe('UpdateGuestPhoneHelper', () => {
  it('should be defined', () => {
    expect(new UpdateGuestPhoneHelper()).toBeDefined();
  });
});

import { UpdateMiniBarHelper } from './update-mini-bar.helper';

describe('UpdateMiniBarHelper', () => {
  it('should be defined', () => {
    expect(new UpdateMiniBarHelper()).toBeDefined();
  });
});

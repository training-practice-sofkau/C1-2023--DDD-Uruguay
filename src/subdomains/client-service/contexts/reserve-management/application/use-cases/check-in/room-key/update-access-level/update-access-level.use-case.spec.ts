import { UpdateAccessLevelUseCase } from './update-access-level.use-case';

describe('UpdateAccessLevelUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateAccessLevelUseCase()).toBeDefined();
  });
});

import { UpdateGuestPhoneUseCase } from './update-guest-phone.use-case';

describe('UpdateGuestPhoneUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateGuestPhoneUseCase()).toBeDefined();
  });
});

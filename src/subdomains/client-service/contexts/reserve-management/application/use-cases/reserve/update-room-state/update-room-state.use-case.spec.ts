import { UpdateRoomStateUseCase } from './update-room-state.use-case';

describe('UpdateRoomStateUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateRoomStateUseCase()).toBeDefined();
  });
});

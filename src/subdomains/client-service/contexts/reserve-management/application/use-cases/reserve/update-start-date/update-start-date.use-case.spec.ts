import { UpdateStartDateUseCase } from './update-start-date.use-case';

describe('UpdateStartDateUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateStartDateUseCase()).toBeDefined();
  });
});

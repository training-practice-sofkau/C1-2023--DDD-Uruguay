import { UpdateNumbreOfGuestUseCase } from './update-numbre-of-guest.use-case';

describe('UpdateNumbreOfGuestUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateNumbreOfGuestUseCase()).toBeDefined();
  });
});

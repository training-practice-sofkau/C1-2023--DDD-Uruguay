import { CreateReserveUseCase } from './create-reserve.use-case';

describe('CreateReserveUseCase', () => {
  it('should be defined', () => {
    expect(new CreateReserveUseCase()).toBeDefined();
  });
});

import { UpdateMiniBarUseCase } from './update-mini-bar.use-case';

describe('UpdateMiniBarUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateMiniBarUseCase()).toBeDefined();
  });
});

import { AddConsumptionUseCase } from './add-consumption.use-case';

describe('AddConsumptionUseCase', () => {
  it('should be defined', () => {
    expect(new AddConsumptionUseCase()).toBeDefined();
  });
});
